// Constant variable, cannot be changed
final Decimal PI;
System.debug(PI);   // Outputs null, because it has no current value

// Initializes variable's value
PI = 3.14159;
System.debug(PI);

// Cannot update the constant variable
// PI = 2.14159;
// System.debug(PI);