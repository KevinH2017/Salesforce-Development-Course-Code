// Constant variable
final Decimal PI;
System.debug(PI);   // Outputs null, because it has no current value

// Initializes variable
PI = 3.14159;
System.debug(PI);

// Cannot update the variable's value
// PI = 2.14159;
// System.debug(PI);